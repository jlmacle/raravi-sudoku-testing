// Needs to be re-tought better
// Cannot foresee where an empty square will be

// describe('US 3.1 - Number addition to grid', () => {
//     it('passes', () => {
//         cy.visit('/');
//         // Clicking an empty square
//         cy.get('tbody > :nth-child(2) > :nth-child(2)').click();
//         // Clicking a number on the number pad
//         cy.get('.status__numbers > :nth-child(2)').click();
//         // Verifying that the value is visible in the square
//         cy.get('tbody > :nth-child(2) > :nth-child(2)').should('have.text', '2');

//     })
// })